// DingLang 网页示例代码
window.DING_EXAMPLES = [
  {
    id: 'hello',
    name: '你好，世界',
    desc: '中文变量、字符串插值',
    code: `# DingLang 示例 —— 中文变量名
let 名字 = "世界"
let 问候 = "你好"

print("{问候}，{名字}！")

# 变量和算术
let x = 10
let y = 3
print("x + y = {x + y}")
print("x * y = {x * y}")

# if 是表达式，有返回值
let 分数 = 85
let 等级 = if 分数 >= 90 { "优" } else if 分数 >= 80 { "良" } else { "及格" }
print("{分数} 分的等级是 {等级}")`,
  },
  {
    id: 'tibetan',
    name: '藏文标识符',
    desc: '变量名、函数名都能用藏文',
    code: `# 藏文变量名（含音节点 ་ tsheg）
let བོད་ཡིག = "藏文"
let བཀྲ་ཤིས་བདེ་ལེགས = "吉祥如意"
let ཐུགས་རྗེ་ཆེ = "谢谢"

print("བོད་ཡིག = {བོད་ཡིག}")
print("བཀྲ་ཤིས་བདེ་ལེགས = {བཀྲ་ཤིས་བདེ་ལེགས}")
print("ཐུགས་རྗེ་ཆེ = {ཐུགས་རྗེ་ཆེ}")

# 藏文变量参与运算
let གཅིག = 1
let གཉིས = 2
let གསུམ = 3
print("གཅིག + གཉིས + གསུམ = {གཅིག + གཉིས + གསུམ}")

# 藏文函数名
fn བསྡོམས(a, b) = a + b
fn ཕྱིར་ཐོན(n) = n * n
print("བསྡོམས(10, 20) = {བསྡོམས(10, 20)}")
print("ཕྱིར་ཐོན(7) = {ཕྱིར་ཐོན(7)}")

# 藏文循环变量
let ཨང་ཀི = []
for ཨང in 1..=10 {
  ཨང་ཀི = ཨང་ཀི + [ཨང * ཨང]
}
print("1到10的平方: {ཨང་ཀི}")`,
  },
  {
    id: 'fib',
    name: '函数与循环',
    desc: '两种函数写法、范围循环、列表',
    code: `# 表达式体函数
fn 平方(n) = n * n

# 块体函数（需要 return）
fn 斐波那契(n) {
  if n <= 1 { return n }
  let a = 0
  let b = 1
  for i in 2..=n {
    let 临时 = a + b
    a = b
    b = 临时
  }
  return b
}

print("平方:")
for i in 1..=10 {
  print("  {i}² = {平方(i)}")
}

print()
print("斐波那契前 15 项:")
let 数列 = []
for i in 0..15 {
  数列 = 数列 + [斐波那契(i)]
}
print("  {数列}")

# 范围循环求和
let 和 = 0
for i in 1..=100 { 和 = 和 + i }
print()
print("1 加到 100 = {和}")

# 范围还能倒着数
let 倒数 = []
for i in 5..=1 { 倒数 = 倒数 + [i] }
print("倒着数: {倒数}")`,
  },
  {
    id: 'list',
    name: '列表操作',
    desc: '遍历、求和、找最大',
    code: `fn 求和(列表) {
  let 总 = 0
  for x in 列表 { 总 = 总 + x }
  return 总
}

fn 最大(列表) {
  let m = 列表[0]
  for x in 列表 {
    if x > m { m = x }
  }
  return m
}

fn 计数(列表, 目标) {
  let n = 0
  for x in 列表 {
    if x == 目标 { n = n + 1 }
  }
  return n
}

let 成绩 = [88, 92, 75, 96, 83, 92, 68, 92, 79, 85]

print("数据:   {成绩}")
print("人数:   {len(成绩)}")
print("总分:   {求和(成绩)}")
print("平均分: {求和(成绩) / len(成绩)}")
print("最高分: {最大(成绩)}")
print("92分人数: {计数(成绩, 92)}")

# 分档
let 优秀 = []
let 其他 = []
for s in 成绩 {
  if s >= 90 { 优秀 = 优秀 + [s] } else { 其他 = 其他 + [s] }
}
print()
print("优秀(≥90): {优秀}")
print("其他:      {其他}")`,
  },
  {
    id: 'tibnum',
    name: '藏文数字',
    desc: '༠-༩ 直接当数字用，还能输出藏文数字',
    code: `# 藏文数字：༠=0 ༡=1 ༢=2 ༣=3 ༤=4 ༥=5 ༦=6 ༧=7 ༨=8 ༩=9
# 和 ASCII 数字完全等价，可以混用

print("=== 藏文数字当字面量 ===")
let པ = ༥
let ཆ = ༧
print("༥ = {པ}")
print("༧ = {ཆ}")

print()
print("=== 参与运算 ===")
print("༥ + ༧ = {༥ + ༧}")
print("༨ * ༢ = {༨ * ༢}")
print("༩ - ༣ = {༩ - ༣}")

print()
print("=== 多位 / 小数 / 混用 ===")
let 年份 = ༢༠༢༦
print("༢༠༢༦ = {年份}")
print("༣.༡༤ = {༣.༡༤}")
print("༡2༣ = {༡2༣}   ← 和 ASCII 混着写也行")

print()
print("=== 和藏文变量名配合 ===")
let བོད་ལོ = ༡༩༥༠
let ད་ལོ = ༢༠༢༦
let ལོ་ཚད = ད་ལོ - བོད་ལོ
print("ལོ་ཚད = {ལོ་ཚད}")

print()
print("=== 范围循环也能用藏文数字 ===")
let ཨང་ཀི = []
for i in ༡..=༥ { ཨང་ཀི = ཨང་ཀི + [i] }
print("༡ 到 ༥: {ཨང་ཀི}")

print()
print("=== 把结果输出成藏文数字 ===")
print("普通: {年份}")
print("藏文: {藏文数字(年份)}")
print("生日: {藏文数字(1990)} 年")
print("圆周率: {藏文数字(3.14)}")`,
  },
  {
    id: 'pureTib',
    name: '★ 纯藏文程序',
    desc: '变量名、函数名、数字全部用藏文写',
    code: `# ═══════════════════════════════════════════════
#   བོད་ཡིག་གི་ཨང་ཀིའི་ལས་ཀ
#   纯藏文程序 —— 变量、函数、数字全用藏文
# ═══════════════════════════════════════════════
#
#   ཨང་ཀི: ༠=0 ༡=1 ༢=2 ༣=3 ༤=4 ༥=5 ༦=6 ༧=7 ༨=8 ༩=9
#   པར()    = 输出
#   བོད་ཨང() = 把数字转成藏文数字

# ── ༡. གཞི་རྩའི་རྩིས་བྱེད  基本算术 ──
let གཅིག = ༡
let གཉིས = ༢
let གསུམ = ༣

པར("གཅིག = {གཅིག}")
པར("གཉིས = {གཉིས}")
པར("གསུམ = {གསུམ}")
པར("")
པར("གཅིག + གཉིས = {གཅིག + གཉིས}")
པར("གསུམ × གཉིས = {གསུམ * གཉིས}")

# ── ༢. ཐོ་གཞུང  列表 ──
let ཨང་ཀི = [༡, ༢, ༣, ༤, ༥, ༦, ༧, ༨, ༩, ༡༠]
པར("")
པར("ཨང་ཀི = {ཨང་ཀི}")
པར("ཨང་ཀིའི་གྲངས = {len(ཨང་ཀི)}")

# ── ༣. བསྡོམས  求和 ──
fn བསྡོམས(ཐོ་གཞུང) {
  let ཁོངས = ༠
  for ཨང in ཐོ་གཞུང {
    ཁོངས = ཁོངས + ཨང
  }
  return ཁོངས
}
པར("བསྡོམས = {བསྡོམས(ཨང་ཀི)}")

# ── ༤. ཆེ་ཤོས / ཆུང་ཤོས  最大 / 最小 ──
fn ཆེ་ཤོས(ཐོ་གཞུང) {
  let མཐོ = ཐོ་གཞུང[༠]
  for x in ཐོ་གཞུང {
    if x > མཐོ { མཐོ = x }
  }
  return མཐོ
}

fn ཆུང་ཤོས(ཐོ་གཞུང) {
  let དམའ = ཐོ་གཞུང[༠]
  for x in ཐོ་གཞུང {
    if x < དམའ { དམའ = x }
  }
  return དམའ
}
པར("ཆེ་ཤོས = {ཆེ་ཤོས(ཨང་ཀི)}")
པར("ཆུང་ཤོས = {ཆུང་ཤོས(ཨང་ཀི)}")

# ── ༥. རང་བཞིན  平方表 ──
fn རང་བཞིན(n) = n * n
པར("")
པར("རང་བཞིན་གྱི་ཐོ་གཞུང:")
for i in ༡..=༡༠ {
  པར("  {i} × {i} = {རང་བཞིན(i)}")
}

# ── ༦. ཆ་གྲངས་དང་ཡ་གྲངས  偶数与奇数 ──
པར("")
པར("ཆ་གྲངས་དང་ཡ་གྲངས:")
for i in ༡..=༡༠ {
  let རིགས = if i % ༢ == ༠ { "ཆ་གྲངས" } else { "ཡ་གྲངས" }
  པར("  {i} ནི {རིགས}")
}

# ── ༧. བོད་ཡིག་གི་ཨང་ཀི  藏文数字输出 ──
པར("")
པར("ལོ = {བོད་ཨང(2026)}")
པར("ཟླ = {བོད་ཨང(12)}")
པར("ཉིན = {བོད་ཨང(25)}")

# ── ༨. ལྟར་བསྒྱུར  倒序输出 ──
པར("")
པར("ལྟར་བསྒྱུར:")
for i in ༡༠..=༡ {
  པར("  {i}")
}`,
  },
  {
    id: 'tibAnalyze',
    name: '★ 藏文文本分析',
    desc: '按音节点 ་ 切分音节，统计并画条形图',
    code: `# 藏文文本分析（网页版）
# 网页里没有文件系统，所以文本直接用三引号字符串内嵌
# 命令行版见 examples/08-藏文分析.ding（那个用 读文件()）

let 音节点 = "་"
let 条形宽 = 30

# ── 要分析的藏文（三引号 = 多行字符串，里面的内容是字面的）──
let 全文 = """
བཀྲ་ཤིས་བདེ་ལེགས།
སངས་རྒྱས་ཀྱི་བསྟན་པ་དར་བར་ཤོག
ཨོཾ་མ་ཎི་པདྨེ་ཧཱུྃ
བོད་ཡིག་ནི་ཧ་ཅང་ཡག་པོ་ཡོད།
ཐུགས་རྗེ་ཆེ་ཆེ།
དགའ་བོ་ཡོད་པ་དགའ་བོ་ཡོད།
གནམ་གྲུ་ནམ་མཁའ་ལ་འཕུར་གྱི་ཡོད།
ཆུ་དང་རི་དང་ཤིང་དང་མེ་ཏོག
སེམས་ཁམས་བཟང་པོ་ཡོད་ན་བདེ་བ་ཡོད།
བཀྲ་ཤིས་བདེ་ལེགས་བཀྲ་ཤིས་བདེ་ལེགས།
ཨོཾ་མ་ཎི་པདྨེ་ཧཱུྃ་ཨོཾ་མ་ཎི་པདྨེ་ཧཱུྃ།
ལྷ་ས་ནི་བོད་ཀྱི་རྒྱལ་ས་ཡིན།
"""

# ── 工具 ──
fn 重复(s, n) {
  let r = ""
  for i in 0..n { r = r + s }
  return r
}
fn 画线(宽) = 重复("─", 宽)
fn 画条(数量, 最大) {
  if 最大 <= 0 { return "" }
  let 格 = 整除(数量 * 条形宽, 最大)
  if 格 < 1 { 格 = 1 }
  return 重复("█", 格)
}
fn 补空格(s, 宽) {
  let 差 = 宽 - len(s)
  if 差 <= 0 { return s }
  return s + 重复(" ", 差)
}

fn 计数(列表) {
  let 统计 = []
  for 项 in 列表 {
    let 找到 = false
    for i in 0..len(统计) {
      if 统计[i][0] == 项 {
        统计[i][1] = 统计[i][1] + 1
        找到 = true
        break
      }
    }
    if 找到 == false { 统计 = 统计 + [[项, 1]] }
  }
  return 统计
}

fn 排序(统计) {
  let n = len(统计)
  for i in 0..n {
    for j in 0..(n - 1) {
      if 统计[j][1] < 统计[j + 1][1] {
        let 临时 = 统计[j]
        统计[j] = 统计[j + 1]
        统计[j + 1] = 临时
      }
    }
  }
  return 统计
}

fn 打印表(标题, 统计, 最多显示, 总数) {
  པར("")
  པར("【{标题}】")
  if len(统计) == 0 { པར("  （没有数据）"); return }
  let 最大 = 统计[0][1]
  let 显示 = 最多显示
  if 显示 > len(统计) { 显示 = len(统计) }
  for i in 0..显示 {
    let 项 = 统计[i][0]
    let 次 = 统计[i][1]
    པར("  {补空格(项, 10)} {补空格(画条(次, 最大), 条形宽)} {补空格(转字符串(次), 3)} {格式化(次 * 100 / 总数, 1)}%")
  }
}

# ══════════════════════════════════════════════
པར("")
པར(画线(58))
པར("  藏文文本分析                   DingLang")
པར(画线(58))

let 行们 = 分行(全文)
let 非空行 = []
for 行 in 行们 {
  if 去空白(行) != "" { 非空行 = 非空行 + [行] }
}

པར("")
པར("【基本信息】")
པར("  总字符数      {len(全文)}")
པར("  非空行数      {len(非空行)}")

# 按音节点切分音节 —— 藏文不用空格分词
let 音节表 = []
for 行 in 非空行 {
  for 音节 in 切分(行, 音节点) {
    let s = 替换(音节, "།", "")
    s = 替换(s, "༎", "")
    s = 去空白(s)
    if s != "" { 音节表 = 音节表 + [s] }
  }
}
let 音节统计 = 排序(计数(音节表))

པར("")
པར("【音节统计】按音节点 ་ 切分")
པར("  音节总数      {len(音节表)}")
པར("  不同音节      {len(音节统计)}")
པར("  平均每行      {格式化(len(音节表) / len(非空行), 1)} 个音节")
པར("  音节重复率    {格式化((len(音节表) - len(音节统计)) * 100 / len(音节表), 1)}%")

打印表("最常用音节 Top 10", 音节统计, 10, len(音节表))

# 字母频次
let 字母表 = []
for 音节 in 音节表 {
  for i in 0..len(音节) {
    let c = 切片(音节, i, i + 1)
    if c != "་" && c != "།" && c != "༎" { 字母表 = 字母表 + [c] }
  }
}
打印表("藏文字母频次 Top 12", 排序(计数(字母表)), 12, len(字母表))

པར("")
པར("【小结】")
པར("  最常用音节    {音节统计[0][0]}  出现 {音节统计[0][1]} 次")
པར("  总音节数      {len(音节表)}")
པར("")
པར(画线(58))
`,
  },
  {
    id: 'tibFile',
    name: '藏文文件分析（读本地文件）',
    desc: '用读文件()读任意文本 —— 需要桌面版或命令行',
    desktopOnly: true,
    code: `# ══════════════════════════════════════════════════════
#   藏文文本分析器
#   读取一个藏文文本文件，统计并可视化
#
#   用法：
#     ding run 藏文分析.ding                         用默认文件
#     ding run 藏文分析.ding -- 我的文本.txt          指定文件
#     ding run 藏文分析.ding -- 我的文本.txt --html   输出到浏览器（藏文渲染更好）
#
#   藏文特色：按「音节点 ་ (tsheg)」切分音节
#             藏文不用空格分词，音节靠 ་ 分隔
# ══════════════════════════════════════════════════════

let 音节点 = "་"
let 条形宽 = 34          # 条形图最大宽度（字符数）
let 线宽 = 62

# ── 工具 ──
fn 重复(s, n) {
  let r = ""
  for i in 0..n { r = r + s }
  return r
}

fn 画线(宽) = 重复("─", 宽)

fn 画条(数量, 最大) {
  if 最大 <= 0 { return "" }
  let 格 = 整除(数量 * 条形宽, 最大)
  if 格 < 1 { 格 = 1 }
  return 重复("█", 格)
}

fn 补空格(s, 宽) {
  let 差 = 宽 - len(s)
  if 差 <= 0 { return s }
  return s + 重复(" ", 差)
}

# ── 统计：把列表里的元素计数，返回 [[元素, 次数], ...] ──
fn 计数(列表) {
  let 统计 = []
  for 项 in 列表 {
    let 找到 = false
    for i in 0..len(统计) {
      if 统计[i][0] == 项 {
        统计[i][1] = 统计[i][1] + 1
        找到 = true
        break
      }
    }
    if 找到 == false {
      统计 = 统计 + [[项, 1]]
    }
  }
  return 统计
}

# ── 排序：按次数从多到少（冒泡）──
fn 排序(统计) {
  let n = len(统计)
  for i in 0..n {
    for j in 0..(n - 1) {
      if 统计[j][1] < 统计[j + 1][1] {
        let 临时 = 统计[j]
        统计[j] = 统计[j + 1]
        统计[j + 1] = 临时
      }
    }
  }
  return 统计
}

# ── 打印一张带条形图的表 ──
fn 打印表(标题, 统计, 最多显示, 总数) {
  པར("")
  པར("【{标题}】")
  if len(统计) == 0 { པར("  （没有数据）"); return }

  let 最大 = 统计[0][1]
  let 显示 = 最多显示
  if 显示 > len(统计) { 显示 = len(统计) }

  for i in 0..显示 {
    let 项 = 统计[i][0]
    let 次 = 统计[i][1]
    # 百分比保留一位小数：次/总数*100
    let 百分比 = 格式化(次 * 100 / 总数, 1)
    པར("  {补空格(项, 12)} {补空格(画条(次, 最大), 条形宽)} {补空格(转字符串(次), 4)} {百分比}%")
  }
}

# ══════════════════════════════════════════════════════
fn 主() {
  # ── 确定文件 ──
  let 文件 = "藏文文本.txt"
  for a in 参数 {
    if 含有(a, ".") { 文件 = a }
  }

  པར("")
  པར(画线(线宽))
  པར("  藏文文本分析                    DingLang 示例")
  པར(画线(线宽))

  if 文件存在(文件) == false {
    པར("")
    པར("  ✗ 找不到文件：{文件}")
    པར("")
    པར("  用法： ding run 藏文分析.ding -- 你的文件.txt")
    པར("")
    return
  }

  # ── 读取 ──
  let 全文 = 读文件(文件)
  པར("  文件：{文件}")

  # ── 基本信息 ──
  let 行们 = 分行(全文)
  let 非空行 = []
  for 行 in 行们 {
    if 去空白(行) != "" { 非空行 = 非空行 + [行] }
  }

  པར("")
  པར("【基本信息】")
  པར("  总字符数      {len(全文)}")
  པར("  总行数        {len(行们)}")
  པར("  非空行数      {len(非空行)}")

  # ── 音节切分：按音节点 ་ 切 ──
  let 音节表 = []
  for 行 in 非空行 {
    for 音节 in 切分(行, 音节点) {
      let s = 音节
      s = 替换(s, "།", "")      # 去掉藏文句号
      s = 替换(s, "༎", "")      # 去掉双句号
      s = 替换(s, " ", "")
      s = 去空白(s)
      if s != "" { 音节表 = 音节表 + [s] }
    }
  }

  let 音节统计 = 排序(计数(音节表))

  པར("")
  པར("【音节统计】按音节点 ་ 切分 —— 藏文不用空格分词")
  པར("  音节总数      {len(音节表)}")
  པར("  不同音节      {len(音节统计)}")
  if len(非空行) > 0 {
    པར("  平均每行      {格式化(len(音节表) / len(非空行), 1)} 个音节")
  }
  if len(音节表) > 0 {
    པར("  音节重复率    {格式化((len(音节表) - len(音节统计)) * 100 / len(音节表), 1)}%")
  }

  # ── 最常用音节 ──
  打印表("最常用音节 Top 12", 音节统计, 12, len(音节表))

  # ── 字母频次 ──
  let 字母表 = []
  for 音节 in 音节表 {
    for i in 0..len(音节) {
      let c = 切片(音节, i, i + 1)
      if c != "་" && c != "།" && c != "༎" && c != " " {
        字母表 = 字母表 + [c]
      }
    }
  }
  let 字母统计 = 排序(计数(字母表))
  打印表("藏文字母频次 Top 15", 字母统计, 15, len(字母表))

  # ── 其他 ──
  let 最长 = ""
  for s in 音节表 {
    if len(s) > len(最长) { 最长 = s }
  }

  པར("")
  པར("【其他】")
  if 最长 != "" {
    པར("  最长音节      {最长}  ({len(最长)} 个字符)")
  }
  if len(音节表) > 0 {
    པར("  平均音节长    {格式化(len(全文) / len(音节表), 2)} 个字符")
  }

  # 找出现次数最多的那个音节单独说一句
  if len(音节统计) > 0 {
    པར("  最常用音节    {音节统计[0][0]}  出现 {音节统计[0][1]} 次")
  }

  པར("")
  པར(画线(线宽))
  པར("")
}

主()
`,
  },
  {
    id: 'error',
    name: '错误处理演示',
    desc: '看看报错信息有多详细',
    code: `# 试着把下面任意一行的注释去掉，看看报错效果

let x = 10

# ① 变量名冲突：藏文音节点和下划线会撞车
# let བོད་ཡིག = 1
# let བོད_ཡིག = 2

# ② 语法错误：括号不匹配
# print(1 + 2

# ③ 语法错误：运算符不是字母
# let y = (+ 1 2)

# ④ 语义错误：break 只能用在循环里
# break

# ⑤ 运行时错误：除以零不会报错，但调用不存在的函数会
# 不存在(1)

print("x = {x}")
print("去掉上面某行的注释，再点运行，看看报错信息")`,
  },
];
